TestFlow Cursor Project Rules

Copy the .cursor folder into the root of the TestFlow repository, then commit it so the team shares the same rules.

All rules use Cursor's .mdc Project Rule format with frontmatter and are always applied because they encode project-wide product, architecture, scope, safety, Git, and communication constraints.

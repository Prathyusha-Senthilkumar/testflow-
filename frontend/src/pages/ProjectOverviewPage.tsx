"use client";

import { Eye, Play, Plus, Sparkles } from "lucide-react";
import { Link, useParams } from "@/lib/navigation";
import { project, recentRuns, suites } from "@/lib/demoData";
import { StatusBadge } from "@/components/common/StatusBadge";
import { PageHeader } from "@/components/common/PageHeader";
export function ProjectOverviewPage(){const {id='demo-project'}=useParams(); const pid=id||'demo-project'; return <div className="p-6 lg:p-8"><PageHeader eyebrow={<><Link to="/projects" className="hover:text-blue-600">Projects</Link> <span className="mx-1">/</span> {project.name}</>} title={project.name} description="Automated visual and interaction suites for regression testing" actions={<><Link to={`/projects/${pid}/test-cases`} className="rounded-md border bg-white px-4 py-2 text-sm font-medium">View Test Cases</Link><Link to={`/projects/${pid}/suites`} className="rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white">View Test Suites</Link></>}/><div className="mt-6 rounded-lg border border-blue-200 bg-blue-50 px-4 py-4"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded bg-blue-600 text-white"><Sparkles size={16}/></span><div><span className="font-semibold">3 suite suggestions are ready for review</span><span className="ml-2 text-sm text-slate-500">AI-detected coverage gaps based on recent admissions portal updates</span></div></div><Link to={`/projects/${pid}/suites/review`} className="inline-flex items-center gap-2 rounded bg-blue-600 px-3 py-2 text-sm font-medium text-white"><Eye size={15}/>Review Suggestions <span className="rounded bg-white/20 px-1.5">3</span></Link></div></div><div className="mt-7 flex items-end justify-between"><div><h2 className="text-xl font-semibold">Test Suites <span className="rounded bg-slate-200 px-1.5 py-0.5 font-mono text-xs font-normal text-slate-600">4 configured</span></h2><p className="mt-1 text-sm text-slate-500">Automated visual and interaction suites for regression testing</p></div><div className="flex gap-2"><Link to={`/projects/${pid}/suites/review`} className="rounded-md border bg-blue-50 px-3 py-2 text-sm font-medium text-blue-700"><Sparkles size={15} className="mr-1 inline"/>Review Suggestions 3</Link><Link to={`/projects/${pid}/suites`} className="rounded-md bg-blue-600 px-3 py-2 text-sm font-medium text-white"><Plus size={15} className="mr-1 inline"/>Create Suite</Link></div></div><div className="mt-4 grid gap-4 xl:grid-cols-2">{suites.map(s=><div key={s.id} className="rounded-lg bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><h3 className="font-semibold">{s.name}</h3><p className="mt-1 font-mono text-xs text-slate-500">{s.cases} Test Cases</p></div><div className="text-right"><div className="text-lg font-bold text-teal-700">{s.passRate}%</div><div className="text-xs text-slate-500">Pass Rate</div></div></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-teal-700" style={{width:`${s.passRate}%`}}/></div><div className="mt-3 flex flex-wrap items-center justify-between gap-3 border-b pb-3 text-xs"><div className="flex gap-4"><span><b className="text-teal-700">{s.passed}</b> Passed</span><span><b className="text-red-600">{s.failed}</b> Failed</span><span><b>{s.notRun}</b> Not Run</span></div><span className="text-slate-500">Last Run: <b className="text-slate-700">{s.lastRun} by {s.lastRunBy}</b></span></div><div className="mt-3 flex justify-end gap-2"><Link to={`/projects/${pid}/suites/${s.id}`} className="rounded border px-3 py-1.5 text-sm">Open Suite</Link><Link to={`/projects/${pid}/runs/104/live`} className="rounded bg-blue-50 px-3 py-1.5 text-sm font-medium text-blue-700"><Play size={13} className="mr-1 inline"/>Run Suite</Link></div></div>)}</div><div className="mt-8 flex items-center justify-between"><h2 className="text-xl font-semibold">Recent Runs</h2><Link to="/runs" className="text-sm text-blue-600">View all runs →</Link></div><div className="mt-3 overflow-hidden rounded-lg bg-white shadow-sm"><table className="w-full text-sm"><thead className="bg-blue-50 text-xs uppercase text-slate-600"><tr><th className="px-5 py-3 text-left">Test / Suite</th><th className="px-5 py-3 text-left">Status</th><th className="px-5 py-3 text-left">Last Run</th><th className="px-5 py-3 text-left">Run By</th><th className="px-5 py-3 text-left">Duration</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody>{recentRuns.slice(0,4).map(r=><tr key={r.id} className="border-t"><td className="px-5 py-4"><div className="font-medium">{r.title}</div><div className="text-xs text-slate-500">{r.subtitle}</div></td><td className="px-5"><StatusBadge status={r.status}/></td><td className="px-5 text-slate-500">{r.executed}</td><td className="px-5">{r.runBy}</td><td className="px-5 font-mono text-xs">{r.duration}</td><td className="px-5 text-right"><Link className="text-blue-600" to={r.status==='Failed'?`/projects/${pid}/results/102?status=failed`:`/projects/${pid}/results/${r.id}`}>{r.status==='Failed'?'View Failure':'View Result'}</Link></td></tr>)}</tbody></table></div></div>}

import type {ReactNode} from "react"; import {cn} from "@/lib/utils"; export function Badge({children,status}:{children:ReactNode;status?:string}){return <span className={cn("inline-flex rounded-full px-2.5 py-1 text-xs font-semibold",status==="Passed"?"bg-emerald-50 text-emerald-700":status==="Failed"?"bg-red-50 text-red-700":"bg-slate-100 text-slate-600")}>{children}</span>}

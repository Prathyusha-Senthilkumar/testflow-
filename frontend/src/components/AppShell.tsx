import {LayoutDashboard,FolderKanban,History,FileBarChart,Settings,FlaskConical,LogOut} from "lucide-react"; import {NavLink,Outlet,useNavigate} from "react-router-dom"; import {supabase} from "@/lib/supabase"; const nav=[["/dashboard","Dashboard",LayoutDashboard],["/projects","Projects",FolderKanban],["/runs","Test Runs",History],["/reports","Reports",FileBarChart],["/settings","Settings",Settings]] as const;
export function AppShell(){const navigate=useNavigate();return <div className="min-h-screen bg-slate-50"><aside className="fixed inset-y-0 left-0 w-60 border-r border-slate-200 bg-white p-4"><div className="mb-8 flex items-center gap-2 px-2 text-lg font-bold"><span className="grid h-9 w-9 place-items-center rounded-lg bg-indigo-600 text-white"><FlaskConical size={19}/></span>TestFlow</div><nav className="space-y-1">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${isActive?"bg-indigo-50 text-indigo-700":"text-slate-600 hover:bg-slate-50"}`}><Icon size={18}/>{label}</NavLink>)}</nav><button onClick={async()=>{await supabase.auth.signOut();navigate("/login")}} className="absolute bottom-5 left-4 flex items-center gap-3 px-3 py-2 text-sm text-slate-500"><LogOut size={18}/>Sign out</button></aside><main className="ml-60 min-h-screen"><Outlet/></main></div>}

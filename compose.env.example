# Optional: copy values into your local backend/.env for non-Docker runs.
# Docker Compose sets REDIS_URL and FRONTEND_URL in docker-compose.yml.

REDIS_URL=redis://localhost:6379/0
FRONTEND_URL=http://localhost:3000
USE_DEMO_DATA=true
NEXT_PUBLIC_API_URL=http://localhost:8000/api
